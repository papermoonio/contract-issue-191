import { RpcServer } from "../types";
export declare class PathToBinariesRpcServer implements RpcServer {
    private readonly nodeBinaryPath;
    private readonly adapterBinaryPath;
    private serverProcess;
    private adapterProcess;
    private serverPort;
    constructor(nodeBinaryPath: string | undefined, adapterBinaryPath: string);
    listen(nodeArgs?: string[], adapterArgs?: string[], blockProcess?: boolean): Promise<void>;
    stop(): Promise<void>;
}
