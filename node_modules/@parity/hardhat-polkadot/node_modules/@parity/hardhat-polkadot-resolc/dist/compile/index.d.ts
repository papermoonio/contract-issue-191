import type { CompilerInput } from "hardhat/types";
import type { SolcOutput } from "@parity/resolc";
import type { CompiledOutput, ResolcConfig } from "../types";
export interface ICompiler {
    compile(input: CompilerInput, config: ResolcConfig): Promise<CompiledOutput | SolcOutput>;
}
export declare function compile(resolcConfig: ResolcConfig, input: CompilerInput): Promise<CompiledOutput | SolcOutput>;
export declare class BinaryCompiler implements ICompiler {
    config: ResolcConfig;
    constructor(config: ResolcConfig);
    compile(input: CompilerInput): Promise<CompiledOutput>;
}
export declare class NpmCompiler implements ICompiler {
    config: ResolcConfig;
    constructor(config: ResolcConfig);
    compile(input: CompilerInput): Promise<SolcOutput>;
}
